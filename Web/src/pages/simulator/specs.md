## Resumen
- Area principal del simulador con canvas y catalogo.

## Que hace
- Combina `SideMenu` (catalogo) y `DiagramWorkspace` (canvas).
- Resuelve `projectId` desde URL o state.
- Permite colapsar el menu lateral en pantallas chicas.

## Diseno UI
- Layout en dos columnas con boton flotante.

## Archivos
- `Simulator.jsx`, `Simulator.css`.
