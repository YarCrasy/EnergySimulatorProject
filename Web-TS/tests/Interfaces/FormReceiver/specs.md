## Resumen
- Pruebas para el formulario de receivers.

## Tests
- `submitNumbersKeepsId.test.jsx`:
  - convierte inputs a number y preserva id al editar,
  - flujo de creación con fallback numérico,
  - callback `onCancel`,
  - mensajes de validación (`onInvalid`/`onInput`).
